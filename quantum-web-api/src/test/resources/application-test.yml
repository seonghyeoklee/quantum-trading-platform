spring:
  datasource:
    url: jdbc:h2:mem:quantum_test
    driver-class-name: org.h2.Driver
    username: sa
    password:
  
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect

# JWT Configuration for Test
jwt:
  secret: test-secret-key-for-quantum-trading-platform-must-be-at-least-512-bits-long-for-hs512
  expiration: 86400

# Logging for Test
logging:
  level:
    com.quantum: DEBUG
    org.springframework.security: DEBUG
    org.hibernate.SQL: DEBUG
    org.springframework.web.servlet.mvc.method.annotation: DEBUG